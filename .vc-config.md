# vc-x1 config file

vc-x1 configuration, the agent side. The `toml` fences, concatenated in document order, form
the config, and [vc-config-model.md](../vc-config-model.md) is the model every table appears in.
Per-key documentation: [vc-config.md](../vc-config.md).

The two repos
- work [[1]]
- agent [[2]]
```toml
[repos]
work = ".."
agent = "."
```

# References

[1]: ../vc-config.md#reposwork
[2]: ../vc-config.md#reposagent
